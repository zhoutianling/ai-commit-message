<idea-plugin>
    <id>io.github.zhoutianling.ai-commit-message</id>
    <name>AI Git Commit Writer</name>
    <vendor email="1396628021@qq.com" url="https://github.com/zhoutianling">zhoutianling</vendor>
    <version>0.3.2</version>
    <description>
        AI Git Commit Writer is a lightweight Git commit message generator for JetBrains IDEs and Android Studio.
        It adds a single action to the Commit panel and uses the user's own OpenAI-compatible API configuration,
        with DeepSeek defaults. The plugin does not require login, does not create a tool window, and does not
        make network requests until the user explicitly clicks an action.
    </description>
    <change-notes>
        v0.3.2: Use COMMIT_WORKFLOW_HANDLER to collect changes (same approach as Qoder CN). Fixes unresponsive click.
    </change-notes>

    <idea-version since-build="261" until-build="263.*"/>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.vcs</depends>

    <extensions defaultExtensionNs="com.intellij">
        <applicationConfigurable
            id="io.github.zhoutianling.ai-commit-message.settings"
            displayName="AI Commit Message"
            instance="com.local.aicommit.AiCommitSettingsConfigurable"/>
        <applicationService serviceImplementation="com.local.aicommit.AiCommitSettingsState"/>
    </extensions>

    <actions>
        <action
            id="io.github.zhoutianling.ai-commit-message.GenerateCommitMessageAction"
            class="com.local.aicommit.GenerateCommitMessageAction"
            text="AI Generate Commit Message"
            description="Generate a commit message from the selected Git changes">
            <add-to-group group-id="Vcs.MessageActionGroup" anchor="first"/>
        </action>
    </actions>
</idea-plugin>